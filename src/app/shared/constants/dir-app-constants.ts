export class DirAppConstants {
    /**
     * Base API url for backend services.
     */
    public static BASE_API: string = "http://localhost:8080";

    /**
     * Retry count for backed hits in case of failure.
     */
    public static RETRY_COUNT:number = 3;
}
